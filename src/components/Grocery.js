
const Grocery = () => {
  return (
    <div>
      <h2>What is Lazy Loading?</h2>
      <p>
        Lazy Loading means loading a component only when it is needed,
        instead of loading all components at the initial page load.
      </p>
    </div>
  );
};

export default Grocery;